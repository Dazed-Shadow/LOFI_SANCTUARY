# Copy to .env (gitignored) and fill in real values.
# .env is NEVER committed.

# Notion — required to run scripts/notion_setup.py
NOTION_API_KEY=secret_REPLACE_ME
NOTION_ROOT_PAGE_ID=REPLACE_WITH_PAGE_UUID
